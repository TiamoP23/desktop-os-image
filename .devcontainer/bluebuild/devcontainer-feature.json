{
	"id": "bluebuild",
	"version": "1.0.0",
	"name": "BlueBuild CLI",
	"documentationURL": "https://github.com/blue-build/cli",
	"description": "Installs the BlueBuild CLI using the upstream installer container via Podman/Docker.",
	"installsAfter": [
		"./podman"
	],
	"options": {
		"version": {
			"type": "string",
			"proposals": [
				"latest"
			],
			"default": "latest",
			"description": "BlueBuild release version/image tag to install (for example, 0.9.35, v0.9.35, or latest)."
		}
	}
}
